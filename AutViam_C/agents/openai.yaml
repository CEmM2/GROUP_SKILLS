interface:
  display_name: "AutViam_C"
  short_description: "Codex phased plan execution"
  default_prompt: "Use $AutViam_C to decompose this plan into phased tasks, scaffold tests, and execute it through gated Codex reviews."

policy:
  allow_implicit_invocation: true
