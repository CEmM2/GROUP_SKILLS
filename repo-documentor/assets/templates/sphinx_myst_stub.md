# Sphinx/MyST notes (stub)

If the repo prefers Sphinx:
- Use MyST Markdown for docs source
- Mirror the `docs/` tree as `source/`
- Enable extensions: myst_parser, napoleon (if Python docstrings), autodoc (optional)

This skill can emit the same Markdown pages; only the build scaffolding differs.